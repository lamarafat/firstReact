function Navbar() {
    return(
        <nav>
        <a href="#">home</a>
        <a href="#">about</a>
        <a href="#">contact</a>
        <a href="#">register</a>
        </nav>
    
)
}
export default Navbar;